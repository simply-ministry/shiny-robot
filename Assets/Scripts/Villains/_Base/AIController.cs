using UnityEngine;

public class AIController : MonoBehaviour
{
    public void SetTarget(GameObject target)
    {
        // Placeholder for setting the AI's target
        Debug.Log($"{gameObject.name} is now targeting {target.name}.");
    }
}
