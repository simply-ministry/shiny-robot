/// <summary>
/// An enum to represent the major transformative states of a character.
/// </summary>
public enum CharacterState
{
    Human,
    VoidTransformed,
    UnifiedEntity // The final state after merging with Era
}