import React from 'react';

export const Card = ({ children, title, index, shadowType }: { children: React.ReactNode, title: string, index: number, shadowType?: string }) => {
    return <div>{children}</div>;
};